<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jetpack.componentization">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.jetpack.baselib.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProxyInstanceActivity"></activity>
        <activity android:name=".RXTestActivity" />
        <activity android:name=".MainActivity3" />
        <activity android:name=".jetpack.DataBindActivity" />
        <activity android:name=".jetpack.JetPackActivity" />
        <activity android:name=".customerview.XfermodeViewActivity" />
        <activity android:name=".customerview.CustomerViewActivity" />
        <activity android:name=".TouchStudyActivity" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.jetpack.baselib.base.InitContentProvider"
            android:authorities="com.jetpack.baselib.base.BaseApplication.cp"
            android:exported="false" />
    </application>

</manifest>